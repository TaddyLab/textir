import(distrom)
import(gamlr)
import(Matrix)

export(corr,
       mnlm,
       normalize,
       pls,
       rdir,
       sdev,
       tfidf)

S3method(plot,pls)
S3method(predict,pls)
S3method(summary,pls)
